# n8n reservation webhook (see app/api/reservations/route.ts).
# Copy this file to .env.local and fill in real values — .env.local is
# gitignored and never committed.
N8N_RESERVATION_WEBHOOK_URL=
N8N_WEBHOOK_SECRET=
